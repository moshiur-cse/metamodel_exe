# minimal environment to install the latest release of hydromt and run the examples
# used for binder
name: metamodel

channels:
  - conda-forge

dependencies:
  - numpy==1.18.5
  - pandas==1.0.5
  - matplotlib==3.2.2
  - python=3.7.7